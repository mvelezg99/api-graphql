// @schemas
import rootSchema from './schema.graphql';
import characterSchema from './character.graphql';
import gameSchema from './game.graphql';

export const schemas = [rootSchema, characterSchema, gameSchema];
